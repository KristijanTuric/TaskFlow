import styled from 'styled-components';
import { motion } from "framer-motion";

export const MainViewContainer = styled(motion.div)`
    background-color: lightblue;
    width: 85vw;
    overflow: hidden;
    display: flex;
    flex-direction: column;
`